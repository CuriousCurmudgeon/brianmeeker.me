---
import urlBuilder from "../utilities/urlBuilder";

const { title, description, date, heroImage, heroImageAlt, tags } =
  Astro.props.post.frontmatter;
const url = urlBuilder(Astro.props.post);
---

<article class="flex flex-col text-black shadow my-4">
  <!-- Article Image -->
  {
    heroImage && (
      <a href="#" class="hover:opacity-75">
        <img src={heroImage} alt={heroImageAlt} />
      </a>
    )
  }
  <div class="bg-white flex flex-col justify-start p-6">
    {
      tags.map((tag) => (
        <a
          href={"/tag/" + tag}
          class="text-blue-700 text-sm font-bold uppercase pb-4"
        >
          {tag}
        </a>
      ))
    }

    <a href={url} class="text-3xl font-bold hover:text-gray-700 pb-4"
      >{title}
    </a>
    <p class="text-sm pb-3">{date}</p>
    <a href={url} class="pb-6">{description}</a>
    <a href={url} class="uppercase text-gray-800 hover:text-black"
      >Continue Reading
    </a>
  </div>
</article>
