{
  "name": "@example/basics",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "astro check && tsc --noEmit && astro dev",
    "start": "astro check && tsc --noEmit && astro dev",
    "build": "astro check && tsc --noEmit && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/tailwind": "^1.0.1",
    "astro": "^1.2.2",
    "tw-elements": "^1.0.0-alpha12"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.7"
  }
}
